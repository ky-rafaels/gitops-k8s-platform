---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: rabbitmq
  namespace: argocd
spec:
  generators:
    - clusters: {} 
  template:
    metadata:
      name: 'rabbitmq'
    spec:
      project: default
      source: 
        repoURL: 'https://gitlab.csde.caci.com/Equinox/belltower-deploy.git' 
        targetRevision: main
        path: k8s/belltower-apps/rabbitmq/kustomize
      destination:
        name: "in-cluster"
        namespace: 'rabbitmq'
      syncPolicy:
        automated:
          prune: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true